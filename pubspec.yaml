name: dtech
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  path: ^1.9.1
  permission_handler: ^12.0.3
  provider: ^6.1.5+1
  shelf: ^1.4.2
  shelf_static: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
